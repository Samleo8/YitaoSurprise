// Probably wanna dynamically load everything?

var pageWidth;

function updatePageWidth(){
    pageWidth = getWidth();

    // Maybe have to update each individual container to size
    // which is kinda sad
    console.log(pageWidth)
}